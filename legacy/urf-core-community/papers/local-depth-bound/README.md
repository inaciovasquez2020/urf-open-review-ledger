# Power Graph Depth Bound

Structural lemma: entropy depth in parallel R-local refinement models
is controlled by the chromatic number and independence number of the
2R-power graph.

Key result:

T ≥ max{ H(X_n)/(c α(G_n^{(2R)})), χ(G_n^{(2R)}) }.
